## Màn hình thông báo kết quả sau mỗi Lượt nộp code (Đúng/Sai)
extends PanelContainer

func display_result(result: Dictionary) -> void:
	# TODO: Tự tay cập nhật Label 
	# Nếu is_correct = true -> Font màu xanh, báo "Bạn đã diệt bug thành công!"
	# Nếu is_correct = false -> Font Đỏ, báo Lỗi của User ở đâu (`result["details"]`)
	# Đủ thời gian (2s) -> Tự động hide() tắt UI này đi 
	pass
